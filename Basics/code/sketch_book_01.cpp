#include "sketch_book_01.h"

#include <string>
#include <vector>

namespace sketchbook
{
	using namespace std;
	




	/*struct my_node
	{
		string
			key,
			value;
		my_node
			* left,
			* rite,
			* branch;

		my_node (string k = "", string v = "", my_node * l = nullptr, my_node * r = nullptr, my_node * b = nullptr) :
			key {k},
			value {v},
			left {l},
			rite {r},
			branch {b}
		{}
	};

	struct my_map
	{
		my_node
			root;

		my_map (my_node r) :
			root {r}
		{}
	};*/
}